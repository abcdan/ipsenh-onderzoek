# Use postgres/example user/password credentials
version: '3.1'

services:
  postgres:
    image: postgres:14.5-alpine
    restart: always
    ports:
      - 5432:5432/tcp
    environment:
      POSTGRES_DB: hemiron
      POSTGRES_PASSWORD: hemiron_security